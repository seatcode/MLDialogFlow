use_frameworks!

target 'MLDialogFlow_Example' do
  pod 'MLDialogFlow', :path => '../'

  target 'MLDialogFlow_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 1.2.0'
    pod 'Nimble', '~> 7.0.2'
  end
end
